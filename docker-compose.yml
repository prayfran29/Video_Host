version: '3.8'

services:
  video-host:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - "${VIDEOS_PATH}:/app/videos"
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - VIDEOS_PATH=${VIDEOS_PATH}
    restart: unless-stopped